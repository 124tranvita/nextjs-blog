export const LIMIT = 5;
export const PAGE_INIT = 1;

export enum Options {
  Yes = "1",
  No = "0",
}

export const Routes = {
  Protected: ["/new", "/edit"],
  Public: ["/", "/post"],
  Auth: ["/login"],
};
