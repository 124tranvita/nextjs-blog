export type Inputs = {
  title: string;
  cover: string;
};

export type PostPreview = {
  title: string;
  cover: string;
  content: string;
};

export const initPostPreview = {
  title: "",
  cover: "",
  content: "",
};
